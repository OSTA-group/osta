import React from 'react'
import { IonGrid, IonImg, IonRow, IonSpinner, IonText } from '@ionic/react'

import './css/StatusScreen.css'

interface LoadingScreenProps {
  text: string
}

export function LoadingIndicator({ text }: LoadingScreenProps) {
  return (
    <IonGrid className="status-container">
      <IonRow>
        <IonImg className="status-container__image" src="/images/loading.png"></IonImg>
      </IonRow>
      <IonRow>
        <IonSpinner color="primary" />
      </IonRow>
      <IonRow>
        <IonText className="status-container__text">{text}</IonText>
      </IonRow>
      <IonRow>
        <IonText className="status-container__credentials">This image was generated by Microsoft CoPilot.</IonText>
      </IonRow>
    </IonGrid>
  )
}
